# In this program, you will learn to check whether a year is leap year or not. We will use single line if...else to solve this problem.

x = int(input("Enter a year :"))

result= "Leap Year" if x%400==0 else "Leap Year" if x%4==0 and x%100!=0 else "Not Leap Year" 

print(result);
